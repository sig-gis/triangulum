name: Clojure CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: tools.deps-builder
      uses: vouch-opensource/tools.deps-build@v1.0.1
